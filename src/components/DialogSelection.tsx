import React from "react";

type DialogSectionProps = {
  handleMode: () => void;
};

export const DialogSelection = (props: DialogSectionProps) => {
  return <dialog>quieres jugar tu solo o en pareja?</dialog>;
};
